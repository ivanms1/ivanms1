query GetUserPosts($page: Int) {
  user(username: "ivanms1") {
    numPosts
    name
    publication {
      title
      posts(page: $page) {
        cuid
        title
        dateAdded
        slug
        brief
        contentMarkdown
      }
    }
  }
}
